{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug MyApp (LLDB)",
      "type": "lldb",                       
      "request": "launch",
      "program": "${workspaceFolder}/Binaries/Mac/MyApp",
      "cwd": "${workspaceFolder}",
      "env": { "DYLD_LIBRARY_PATH": "${workspaceFolder}/Binaries/Mac" },
      "preLaunchTask": "Build MyApp"
    }
  ]
}
